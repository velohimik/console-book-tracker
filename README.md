# Console Book Tracker

Console application for managing your books. I developed it during the Java study. For now user can add a new books, get the list of the existing books and get one book by id. The book list is stored in the memory. User can interact with the app in the terminal with simple console menu 

## Run

- execute ```library.sh``` in the root;
- use the app according to the instruction in the terminal.